name: Deployment Exercise 1 (master)
on: push
jobs:
  deploy:
    runs-on: cicd-t
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: echo "thao2" > test2.txt
      - name: Lint
        run: cat test2.txt
      - name: Deploy code
        run: ls -l
