name: Call reusable workflow

on:
  push:

env:
  BUILD_LINK: "fromJSON({'false': '${GITHUB_REF:11:3}', 'true': 'dev'})[${GITHUB_REF:11:7} =='develop']"

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: test
        run: 'echo ${{ env.BUILD_LINK }} '
  slack-notification:
    name: Slack notification
    needs: build
    uses: ./.github/workflows/reuse.yaml
    with:
      message: 'aaaaa'
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_BOT_TOKEN }}